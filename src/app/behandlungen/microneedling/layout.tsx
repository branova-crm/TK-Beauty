import { pageMetadata } from "@/lib/seo/metadata";

export const metadata = pageMetadata.microneedling;

export default function MicroneedlingLayout({ children }: { children: React.ReactNode }) {
    return children;
}
