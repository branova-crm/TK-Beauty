import { pageMetadata } from "@/lib/seo/metadata";

export const metadata = pageMetadata.kosmetik;

export default function KosmetikLayout({ children }: { children: React.ReactNode }) {
    return children;
}
