import { pageMetadata } from "@/lib/seo/metadata";

export const metadata = pageMetadata.behandlungen;

export default function BehandlungenLayout({ children }: { children: React.ReactNode }) {
    return children;
}
