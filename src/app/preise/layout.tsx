import { pageMetadata } from "@/lib/seo/metadata";

export const metadata = pageMetadata.preise;

export default function PreiseLayout({ children }: { children: React.ReactNode }) {
    return children;
}
