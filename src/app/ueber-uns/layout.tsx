import { pageMetadata } from "@/lib/seo/metadata";

export const metadata = pageMetadata.ueberUns;

export default function UeberUnsLayout({ children }: { children: React.ReactNode }) {
    return children;
}
